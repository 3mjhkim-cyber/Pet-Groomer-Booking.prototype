{
  "version": 2,
  "buildCommand": "npx vite build && npx esbuild api/_handler.ts --bundle --platform=node --format=cjs --outfile=api/index.js",
  "outputDirectory": "dist/public",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
