{
  "version": 2,
  "buildCommand": "npx vite build && npx esbuild api/index.ts --bundle --platform=node --format=cjs --outfile=api/index.js --banner:js=\"/* auto-built */\"",
  "outputDirectory": "dist/public",
  "functions": {
    "api/index.js": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.js" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
